#include <WiFi.h>
#include <HTTPClient.h>

const char* ssid = "globalsoft GOST";
const char* password = "Globalsoft2023!";
const char* serverUrl = "192.168.1.180:3000/post-data";

void setup() {
  Serial.begin(115200);
  WiFi.begin(ssid, password);

  while (WiFi.status() != WL_CONNECTED) {
    delay(1000);
    Serial.println("Spajanje...");
  }
  Serial.println("Spojen na WiFi mrežu!");
}

void loop() {
  if (WiFi.status() == WL_CONNECTED) {
    HTTPClient http;
    http.begin(serverUrl);

    int vrijednostSenzora = 123456789;  
    String postData = "sensor=" + String(vrijednostSenzora);

    http.addHeader("Content-Type", "application/x-www-form-urlencoded");
    int httpResponseCode = http.POST(postData);

    if (httpResponseCode > 0) {
      String response = http.getString();
      Serial.println(response);
    } else {
      Serial.print("Greška: ");
      Serial.println(httpResponseCode);
    }
    http.end();
  }
  delay(10000);  
}
